max_epochs: 8
accelerator: auto
devices: 1
log_every_n_steps: 10
# Clip gradient norm — standard first-line mitigation for AMP (fp16) producing NaN.
# Harmless at fp32/on CPU/MPS too, so it's a safe default everywhere, not just Kaggle.
gradient_clip_val: 1.0
